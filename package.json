{
  "name": "dockhand-lite",
  "version": "0.0.1",
  "author": "BoxBoat",
  "bin": {
    "dockhand-lite": "./bin/run"
  },
  "bugs": "https://gitlab.com/boxboat/dockhand/gitlab-poc/dockhand-lite/issues",
  "dependencies": {
    "@oclif/command": "^1",
    "@oclif/config": "^1",
    "@oclif/plugin-help": "^3",
    "ajv": "^6.12.5",
    "tslib": "^1"
  },
  "devDependencies": {
    "@oclif/dev-cli": "^1",
    "@oclif/test": "^1",
    "@types/chai": "^4",
    "@types/mocha": "^5",
    "@types/node": "^10",
    "chai": "^4",
    "eslint": "^7.9",
    "eslint-config-oclif": "^3.1",
    "eslint-config-oclif-typescript": "^0.2",
    "globby": "^10",
    "mocha": "^5",
    "nyc": "^14",
    "oclif": "^1.16.1",
    "ts-node": "^8",
    "typescript": "^3.3",
    "typescript-json-schema": "^0.43.0"
  },
  "engines": {
    "node": ">=8.0.0"
  },
  "files": [
    "/bin",
    "/lib",
    "/npm-shrinkwrap.json",
    "/oclif.manifest.json"
  ],
  "homepage": "https://gitlab.com/boxboat/dockhand/gitlab-poc/dockhand-lite",
  "keywords": [
    "oclif"
  ],
  "license": "Apache-2.0",
  "main": "lib/index.js",
  "oclif": {
    "commands": "./lib/commands",
    "bin": "dockhand-lite",
    "plugins": [
      "@oclif/plugin-help"
    ]
  },
  "repository": "https://gitlab.com/boxboat/dockhand/gitlab-poc/dockhand-lite",
  "scripts": {
    "cli": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme && ./bin/run",
    "eslint-fix": "eslint . --ext .ts --config .eslintrc --fix",
    "generate-schema": "node ./node_modules/typescript-json-schema/bin/typescript-json-schema 'src/spec/**/*.ts' '*' -o src/spec/schema.json",
    "oclif": "oclif",
    "postpack": "rm -f oclif.manifest.json",
    "posttest": "eslint . --ext .ts --config .eslintrc",
    "prepack": "rm -rf lib && tsc -b && oclif-dev manifest && oclif-dev readme",
    "test": "nyc --extension .ts mocha --forbid-only \"test/**/*.test.ts\"",
    "version": "oclif-dev readme && git add README.md"
  },
  "types": "lib/index.d.ts"
}
