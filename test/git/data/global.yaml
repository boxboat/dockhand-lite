artifactRepoMap:
  default:
    host: index.docker.io

buildVersionsRepo:
  gitConnectionKey: local
  path: build-versions.git

gitConnectionMap:
  local:
    authorName: dockhand
    authorEmail: dockhand@example.com
    pathPrefix: test/git/data/repos
  gitHub:
    authorName: dockhand
    authorEmail: dockhand@example.com
    remoteHost: github.com
    remoteProtocol: ssh
    remoteUser: git
    pathPrefix: boxboat
